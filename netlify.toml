[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Trailing-slash URLs → canonical paths (matches trailingSlash: "never")
[[redirects]]
  from = "/*/"
  to = "/:splat"
  status = 301
  force = false
